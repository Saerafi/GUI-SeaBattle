import java.awt.*;
import javax.swing.*;

public class Mywindow extends JFrame {
    public Mywindow() {
        super("MyWindow");
        setSize(300, 100);
        setLocation(200, 200);
        setLayout(null);

        setVisible(true);
    }
}
